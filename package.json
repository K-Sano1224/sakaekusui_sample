{
  "name": "civic-band-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "pages:deploy": "npm run build && wrangler pages deploy dist --project-name=sakaekusui"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "astro": "^6.3.4"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
